# API Configuration
# Main API URL - Used by bonuses, bulletins, and other services
# The code will try these variables in order:
# 1. NEXT_PUBLIC_API_URL (recommended)
# 2. PUBLIC_API_BASE_URL (legacy)
# 3. API_BASE (legacy)
# 4. Fallback to https://api.sitimm.org/api
NEXT_PUBLIC_API_URL=https://api.sitimm.org/api

# Site Configuration
NEXT_PUBLIC_SITE_NAME=SITIMM Info
NEXT_PUBLIC_SITE_URL=http://localhost:3031

# JWT Token for API authentication (if required)
SITIMM_API_JWT=token TU_TOKEN_AQUI

# ConfigCat Feature Flags
# Get your SDK key from https://app.configcat.com/
# Sign up for free at https://configcat.com/
NEXT_PUBLIC_CONFIGCAT_SDK_KEY=your-sdk-key-here

# Sentry Error Tracking
# Get your DSN from https://sentry.io/
# Sign up for free at https://sentry.io/
# Leave empty to disable Sentry in development
NEXT_PUBLIC_SENTRY_DSN=
# Optional: Sentry Auth Token for source maps upload (CI/CD only)
SENTRY_AUTH_TOKEN=

# Botpress Chatbot Configuration
# Get your Bot ID from Botpress Cloud (https://app.botpress.cloud/)
# Sign up for free at https://botpress.com/
# Leave empty to disable the chatbot
#
# For Botpress v3.4:
# - Bot ID format: YYYYMMDDHHMMSS-XXXXXXXX (e.g., 20251108190059-6BB87X6P)
# - Find it in your deploy code at Botpress Cloud dashboard
# - The host URL is set to v3.4 by default
NEXT_PUBLIC_BOTPRESS_BOT_ID=
NEXT_PUBLIC_BOTPRESS_HOST_URL=https://cdn.botpress.cloud/webchat/v3.4
NEXT_PUBLIC_BOTPRESS_MESSAGING_URL=
NEXT_PUBLIC_BOTPRESS_CLIENT_ID=

# Google Analytics Configuration
# Get your Measurement ID from Google Analytics 4 (https://analytics.google.com/)
# 1. Go to https://analytics.google.com/
# 2. Create a GA4 property (Admin → Create Property)
# 3. Add a Web Data Stream (Admin → Data Streams → Add stream → Web)
# 4. Copy the Measurement ID (format: G-XXXXXXXXXX)
# 5. Add it here and restart the dev server
# Leave empty to disable Google Analytics
NEXT_PUBLIC_GA_MEASUREMENT_ID=

# Analytics Endpoint (Optional)
# Custom endpoint to receive Web Vitals data (for custom tracking)
# Leave empty to only send Web Vitals to Google Analytics (if configured above)
NEXT_PUBLIC_ANALYTICS_ENDPOINT=
